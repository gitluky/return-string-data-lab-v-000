<% @products.each do |product| %>
  <%= product.name %>
  <%= product.price %>
  <%= button_tag 'More Info', class: 'more-info', data: { productId: "#{product.id}" } %>
<% end %>

<script type="text/javascript" charset="utf-8" >
  

</script>
